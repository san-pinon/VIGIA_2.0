[Unit]
Description=VIGIA 2.0 — capture visible-spectrum images
Wants=capture-vis.timer

[Service]
User=user
Type=oneshot
WorkingDirectory=/home/user
ExecStart=/home/user/.venv/bin/multicamctl capture visible

[Install]
WantedBy=multi-user.target
