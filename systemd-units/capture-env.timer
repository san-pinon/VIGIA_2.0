[Unit]
Description=VIGIA 2.0 — trigger environmental capture every minute
Requires=capture-env.service

[Timer]
Unit=capture-env.service
OnCalendar=*-*-* *:*:00
AccuracySec=1s

[Install]
WantedBy=timers.target
